apiVersion: apps/v1
kind: Deployment
metadata:
  name: messaging-service
  namespace: hiresphere
spec:
  replicas: 2
  selector:
    matchLabels:
      app: messaging-service
  template:
    metadata:
      labels:
        app: messaging-service
    spec:
      containers:
        - name: messaging-service
          image: 793424482103.dkr.ecr.ap-southeast-1.amazonaws.com/hiresphere/messaging-service:latest
          ports:
            - containerPort: 3004
          envFrom:
            - configMapRef:
                name: hiresphere-config
            - secretRef:
                name: hiresphere-secrets
          env:
            - name: PORT
              value: "3004"
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: hiresphere
spec:
  selector:
    app: messaging-service
  ports:
    - port: 80
      targetPort: 3004
